{
  "$comment": "Domains that must never be published as catalog records. Honored by the sync (scripts/batch/sync-kv.ts), the build (scripts/normalize.ts), the site's render paths, and the worker's discovery write path. Add an entry instead of deleting a domains/ file: a plain deletion is undone by the next KV sync.",
  "domains": [
    {
      "domain": "slidesgpt-server-uojeu24xoq-uc.a.run.app",
      "reason": "Opaque Cloud Run host duplicating the vendor record at slidesgpt.com (mcp.slidesgpt.com). Its only evidence was an old integrations.sh page.",
      "addedAt": "2026-09-21"
    }
  ]
}
